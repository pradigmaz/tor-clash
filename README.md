# Tor IP Rotator

Утилита для Windows с графическим интерфейсом для автоматической смены IP-адреса через связку Tor и Clash.

### Ключевые возможности

- **🎨 Современный интерфейс:** Material Design (KivyMD)
- **🔐 Безопасность:** Конфигурация хранится в `.env` файле
- **⚙️ Стабильность:** Оптимизированная работа с потоками и процессами
- **🔄 Автоматическая ротация:** Настраиваемый интервал смены IP

***

### Быстрый старт

**1. Требования:**

* Python 3.10+
* [Tor Expert Bundle](https://www.torproject.org/download/tor/) (скачайте Windows Expert Bundle, распакуйте .tar.gz)
* [Clash for Windows](https://www.clashforwindows.net/clash-for-windows-download/)

**2. Установка:**

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd tor-rotator

# Установите зависимости
pip install -r requirements.txt
```

**3. Запуск:**

```bash
python Rotator.py
```

**При первом запуске:**
* Автоматически откроется мастер настройки с кнопками для скачивания Tor/Clash
* **Важно:** Tor скачивается как `.tar.gz` — распакуйте архив (например, через 7-Zip)
* Нажмите «Автопоиск» — приложение найдёт Tor и Clash на вашем ПК
* Или укажите пути вручную
* Приложение автоматически создаст:
  - Случайный пароль и его хеш
  - `torrc` с хешированным паролем
  - `global.txt` (конфиг Clash)
  - `.env` с путями и паролем

***

### Как пользоваться

* **Автоматическая ротация:** Включите переключатель, задайте интервал — приложение будет менять IP в фоновом режиме
* **Ручная смена:** Нажмите «Сменить IP сейчас» для мгновенного получения нового адреса
* **Копирование IP:** Нажмите на иконку рядом с адресом, чтобы скопировать его
* **Перенастройка:** Кнопка ⚙ — пересоздать конфиги и пароль (старые сохраняются в бэкап)

***

### Структура проекта

```
.
├── core/                      # Ядро приложения
│   ├── config.py             # Конфигурация из .env
│   ├── process_manager.py    # Управление процессами Tor/Clash
│   ├── ip_checker.py         # Проверка IP адреса
│   └── proxy_manager.py      # Управление системным прокси
├── Rotator.py                # Главное приложение (UI)
├── .env.example              # Пример конфигурации
└── requirements.txt          # Зависимости Python
```

***

### Для разработчиков

* **Архитектура:** Разделение на ядро (`core/`) и UI (`Rotator.py`)
* **Безопасность:** `.env` включен в `.gitignore`
* **Оптимизация:** `ThreadPoolExecutor` для фоновых задач, `threading.Event` для эффективного ожидания

***

**Лицензия:** MIT
